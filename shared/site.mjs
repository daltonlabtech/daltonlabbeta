/**
 * Fonte única do host canônico. Reusado pelo gerador de sitemap
 * (scripts/generate-sitemap.mjs) e pelo servidor de runtime (server.mjs),
 * para o canonical do sitemap e o redirect apex→www nunca divergirem.
 */
export const SITE_URL = 'https://www.daltonlab.ai'
